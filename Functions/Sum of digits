## Sum of digits

def sumi(n):
    total = 0
    while n > 0:
        digit = n % 10
        total += digit
        n //= 10
    return total

n = int(input("Enter the number: "))
print("Sum of digits:", sumi(n))
